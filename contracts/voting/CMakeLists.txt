cmake_minimum_required(VERSION 3.5)
project(voting VERSION 1.0.0)
find_package(eosio.cdt)
add_contract( voting voting voting.cpp)
target_compile_options( voting.wasm PUBLIC -R${CMAKE_CURRENT_SOURCE_DIR} )